# ============================================================================
# docker-compose.env.example - Template per variabili d'ambiente Docker Compose
# ============================================================================
# Copia questo file come docker-compose.env e compila i valori reali
# Usa questo file con: docker-compose --env-file docker-compose.env up
# MAI committare il file docker-compose.env con valori reali!

# ============================================================================
# DATABASE CREDENTIALS
# ============================================================================
# Credenziali database PostgreSQL
# In produzione, usa un secret manager o Docker secrets
POSTGRES_DB=myplanner
POSTGRES_USER=myplanner
POSTGRES_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# ============================================================================
# BACKEND CONFIGURATION
# ============================================================================
# SECRET_KEY è OBBLIGATORIA e deve essere almeno 32 caratteri
# Genera con: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=GENERATE_32_CHAR_RANDOM_STRING_HERE

# Database URL per il backend (usa le credenziali sopra)
DATABASE_URL=postgresql://myplanner:CHANGE_ME_SECURE_PASSWORD@db:5432/myplanner

# Porte esposte
DB_PORT=5432
BACKEND_PORT=8000
FRONTEND_PORT=3000

# ============================================================================
# FRONTEND CONFIGURATION
# ============================================================================
# URL API backend per il frontend (build-time variable)
VITE_API_URL=http://localhost:8000

# ============================================================================
# ORACLE CLOUD CONFIGURATION (Production only)
# ============================================================================
# Namespace OCIR (dalla tua tenancy Oracle)
# Ottenibile da: Developer Services → Container Registry → Namespace
OCIR_NAMESPACE=your-tenancy-namespace

# CORS origins per produzione (separati da virgola)
# Esempio per Oracle VM: http://158.101.xxx.xxx
CORS_ALLOWED_ORIGINS=http://YOUR_VM_IP

# Workers Uvicorn per produzione (consigliato: numero di CPU cores)
UVICORN_WORKERS=4

# Porta frontend in dev mode (Vite hot-reload)
FRONTEND_DEV_PORT=5173

# Ambiente di esecuzione (development o production)
ENVIRONMENT=development

# ============================================================================
# NOTE SICUREZZA
# ============================================================================
# - POSTGRES_PASSWORD e SECRET_KEY sono CRITICI: proteggere con cura
# - In produzione, usa Docker secrets o variabili d'ambiente del sistema
# - Non committare mai questo file con valori reali!
# - Per Docker Swarm: usa 'docker secret create' invece di env file

